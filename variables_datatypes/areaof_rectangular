#include <stdio.h>

int main()
{
    int l,b,area;
    printf("enter length =%d",l);
    scanf("%d",&l);
    printf("enter breadth=%d",b);
    scanf("%d",&b);
    area=l*b;
    printf("area of rectangular is %d",area);
    return 0;
 
 
}
