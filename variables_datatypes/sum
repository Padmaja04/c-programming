#include <stdio.h>

int main()
{
    int a,b,sum;
    printf("enter first number=%d",a);
    scanf("%d",&a);
    printf("enter second number=%d",b);
    scanf("%d",&b);
    sum=a+b;
    printf("sum of two numbers is %d",sum);
    return 0;
 
 
}
